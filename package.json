{
  "name": "murphys-laws",
  "version": "0.1.0",
  "private": true,
  "description": "Local dev server setup for Murphy's Laws static site",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "vite": "^7.1.0"
  }
}
